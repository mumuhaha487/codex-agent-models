interface:
  display_name: "CustomAgent 模型配置"
  short_description: "仅写 CustomAgent.toml，配置可写子智能体并提供隔离回退"
  default_prompt: "使用 $deepseek，通过本机页面配置模型、思考强度和识图能力；继承当前 URL 与 Key，只写 CustomAgent.toml，并在隔离 Git worktree 中安全修改代码。"
